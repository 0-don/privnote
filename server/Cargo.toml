[package]
name = "priv_note_server"
version = "1.0.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[workspace]
members = [".", "api", "migration", "service", "entity"]


[dependencies]
priv_note_api = { path = "api" }
